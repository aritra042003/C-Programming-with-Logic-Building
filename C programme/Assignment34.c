#include <stdio.h>
int main()
{
    // Q1
    //  char str[20];
    //  int i=0,count=0;
    //  fgets(str,10,stdin);
    //  while(str[i]!='\0'){
    //      count++;
    //      i++;
    //  }
    //  printf("length of this string is  : %d",count-1);
    // Q2
    // char str[20], d;
    // int i = 0, count = 0;

    // printf("enter the string : ");
    // fgets(str, 10, stdin);
    // printf("enter the character: ");
    // scanf("%c", &d);
    // for (i = 0; i < 10; i++)
    // {
    //     if (d == str[i])
    //         count++;
    // }
    // printf("occurrence of this '%c' in this string is : %d ", d, count);
    //q3
    //  char str[20], d;
    // int i = 0, count = 0;

    // printf("enter the string : ");
    // fgets(str, 10, stdin);
    // for (i = 0; i < 10; i++)
    // {
    //     if (str[i]=='a'||str[i]=='e'||str[i]=='i'||str[i]=='o'||str[i]=='u')
    //         count++;
    // }
    // printf("no of vowels in this string is : %d",count);
    //  char str[20], d;
    // int i = 0, count = 0;

    // printf("enter the string : ");
    // fgets(str, 10, stdin);
    // for (i = 0; i < 10; i++)
    // {
    //     if (str[i]==' ')
    //         count++;
    // }
    // printf("no of spaces  in this string is : %d",count);
    //  char str[20] ;
    // int i = 0;

    // printf("enter the string : ");
    // fgets(str, 10, stdin);
    // for (i = 0; i < 10; i++)
    // {
    //    str[i]=str[i]-32;
    // }
    // printf("uppercase of  this string is : %s",str);
}