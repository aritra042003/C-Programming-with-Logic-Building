#include <stdio.h>
int main()
{ // Q1
    // int i, x;
    // for (x = 2; x < 100; x++)
    // {
    //     for (i = 2; i < x; i++)
    //         if (x % i == 0)
    //             break;
    //     if (i == x)
    //         printf("%d ", x);
    // }
    // Q2
    // int i, x, a, b;
    // printf("enter the given range : ");
    // scanf("%d %d", &a, &b);
    // for (int x = a + 1; x < b; x++)

    // {
    //     for (i = 2; i < x; i++)
    //         if (x % i == 0)
    //             break;
    //     if (i == x)
    //         printf("%d ", x);
    // }
    // Q3
    // int n1, count;
    // printf("enter the number : ");
    // scanf("%d", &n1);
    // for (int i = n1; i > 0; i++)
    // {
    //     for (int j = 2; j < i; j++)
    //     {
    //         if (i % j == 0)
    //         {
    //             count++;
    //             break;
    //         }
    //     }
    //     if (count == 0)
    //     {
    //         printf(" next prime of %d is : %d ", n1, i);
    //         break;
    //     }
    //     count = 0;
    // }
    // Q4
    // int  a, b, H;
    // printf("enter  two numbers : ");
    // scanf("%d %d",&a,&b);
    // for (H = a < b ? a : b; H >= 1; H--)
    // {
    //     if (a % H == 0 && b % H == 0)
    //         break;
    // }
    // printf("HCF is %d ", H);
    // Q5
    // int a, b, H;
    // printf("enter  two numbers : ");
    // scanf("%d %d", &a, &b);
    // for (H = a < b ? a : b; H >= 1; H--)
    // {
    //     if (a % H == 0 && b % H == 0)
    //         break;
    // }
    // if (H == 1)
    // {
    //     printf("%d and %d are co prime numbers ", a, b);
    // }
    // else
    // {
    //     printf("%d and %d are  not co prime numbers ", a, b);
    // }
    
}