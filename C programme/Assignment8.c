#include <stdio.h>
#include <math.h>
int main()
{ // Q1
   /*int num;
   printf("enter a number : ");
   scanf("%d", &num);

   if (abs(num) / 100 > 0 && abs(num) / 1000 == 0)
   {
      printf("number is 3 digit");
   }
   else
   {
      printf("number is not  3 digit");
   }
*/

   // Q2
   /*int a, b;
   printf("enter two numbers : ");
   scanf("%d %d", &a, &b);
   if (a > b)
      printf("%d is greater ", a);
   else if (a == b)
      printf("numbers are same ");
   else
      printf("%d is greater ", b);
   */
   // Q3
   /*int a, b, c, D;
   printf("enter the values a  b  c : ");
   scanf("%d %d %d", &a, &b, &c);
   D = (b * b) - 4 * a * c;
   if (D > 0)
      printf(" value of D is : %d , so roots are real and distinct", D);
   else if (D == 0)
      printf(" value of D is : %d , so roots are real and Equal", D);
   else
      printf(" value of D is : %d , so roots are Imaginary", D);
    */

   // Q4
   /*int year;
   printf("enter year : ");
   scanf("%d", &year);
   if (year % 4==0 || year % 400==0)
      printf("%d is a leap year ", year);
   else
      printf("%d is not a leap year ", year);
      */
   // Q5
   /*int a, b, c;
   printf("enter three numbers : ");
   scanf("%d %d %d", &a, &b, &c);
   if (a > b && a > c)
      printf(" %d is greater ", a);
   else if (b > a && b > c)
      printf(" %d is greater ", b);
   else if (a == b == c)
      printf("all are same ");
   else
      printf("%d is greater ", c);
   */
   return 0;
}